# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.env
.venv
env/
venv/
pip-log.txt
*.egg-info/
dist/
build/

# ML models (large binaries) — except the small nano model used by the
# opt-in detection build, which is committed so the image is reproducible.
*.pt
*.pth
*.onnx
*.engine
*.weights
!yolov8n.pt

# Data files
data/*.db
data/retail.db*
data/frames/
data/events*.jsonl
*.jsonl
# Pre-computed detection output IS committed so `docker compose up` works
# without re-running the heavy YOLO pipeline.
!data/events_real.jsonl
# POS data committed as sample (not gitignored)
# data/pos_transactions.csv is tracked

# Docs planning notes (personal, not submission)
docs/plan.md
docs/MASTER_PLAN.md
docs/decisions/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test artifacts
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Temp
tmp/
temp/
